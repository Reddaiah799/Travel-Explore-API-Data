{
  "name": "travel-explore-api-data",
  "version": "1.0.0",
  "description": "Fake REST API for Travel Explore App",
  "main": "index.js",
  "scripts": {
   "start": "json-server --watch db.json --port $PORT"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
